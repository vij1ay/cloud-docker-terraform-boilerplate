pipeline {
  agent any

  stages {

    stage('Terraform Init') {
      steps {
        dir("aws-terraform") {
          sh 'terraform init'
        }
      }
    }

    stage('Terraform Apply') {
      steps {
        dir("aws-terraform") {
          sh 'terraform apply -auto-approve'
        }
      }
    }
  }
}
